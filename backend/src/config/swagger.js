const swaggerJsdoc = require('swagger-jsdoc');
require('dotenv').config();

const options = {
  definition: {
    openapi: '3.0.0',
    info: {
      title: 'EpicEvents API Documentation',
      version: '1.0.0',
      description: 'API Documentation for EpicEvents Admin Panel Registration and Login Flow',
    },
    servers: [
      {
        url: `http://localhost:${process.env.PORT || 3000}`,
      },
    ],
    components: {
      securitySchemes: {
        bearerAuth: {
          type: 'http',
          scheme: 'bearer',
          bearerFormat: 'JWT',
        },
      },
    },
  },
  apis: ['./src/routes/*.js'], // Look for swagger comments in the routes directory
};

const specs = swaggerJsdoc(options);

module.exports = specs;
